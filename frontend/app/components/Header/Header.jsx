import React from 'react'
import styles from '@/app/components/Header/header.module.sass';

export default function Header() {
  return (
    <header className = {`${styles.header} mt-70`}>
        <div className={styles .header_container}>
            Hello
        </div>
        
    </header>
  )
}
